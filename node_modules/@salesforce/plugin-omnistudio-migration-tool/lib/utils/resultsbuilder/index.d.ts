import { MigratedObject } from '../interfaces';
export declare class ResultsBuilder {
    static generate(results: MigratedObject[], instanceUrl: string): Promise<void>;
    private static generateDocument;
    private static generateResult;
}
