import { Connection } from '@salesforce/core';
import { AnyJson } from '@salesforce/ts-types';
export declare class QueryTools {
    static buildCustomObjectQuery(namespace: string, name: string, fields: string[], filters?: Map<string, any>): string;
    static buildCustomObjectFields(namespace: string, fields: string[]): string[];
    static queryAll(connection: Connection, namespace: string, objectName: string, fields: string[]): Promise<AnyJson[]>;
    static queryWithFilter(connection: Connection, namespace: string, objectName: string, fields: string[], filters?: Map<string, any>): Promise<AnyJson[]>;
    static queryWithFilterAndSort(connection: Connection, namespace: string, objectName: string, fields: string[], filters?: Map<string, any>, orderBy?: Array<SortField>): Promise<AnyJson[]>;
    static query(connection: Connection, objectName: string, fields: string[], filters?: Map<string, any>, orderBy?: Array<SortField>): Promise<any[]>;
    static queryIds(connection: Connection, objectName: string, filters?: Map<string, any>): Promise<string[]>;
    private static getFilterValue;
}
export declare enum SortDirection {
    ASC = "asc",
    DESC = "desc"
}
export interface SortField {
    field: string;
    direction: SortDirection;
}
